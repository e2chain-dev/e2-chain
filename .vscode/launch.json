{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Linux Launch",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceRoot}/target/debug/e2-chain",
      "args": ["--dev", "-d .chaindata", "--execution=native"],
      "cwd": "${workspaceRoot}"
    }
  ]
}
